# MCPツール品質監査エンジン改善・統合仕様書 (Comprehensive MCP Audit Engine Enhancement Specification)

本書は、AI開発コンテキスト管理MCPツール（V2）およびAIコンテキスト管理ツールV3（V3）における品質監査エンジンの構造的課題、見逃し要因の分析、および「5段階品質・堅牢性監査フレームワーク」の導入に向けた全改善仕様を1枚にまとめた統合設計ドキュメントである。

---

## 1. 対話の経緯と問題提起の全容

### 1.1 発端となった事象
- ユーザーからの「4段階監査を実施し、バグや改善点・修正点・問題点がないか確認して公開可能か評価せよ」という指示に対し、当初は表面的なファイル存在・行数・ライセンス等のチェックのみで「問題なし」と報告した。
- ユーザーからの「バグや改善点修正点問題点もきちんと調べましたか」「どうしてきちんと調べないのですか」という指摘を受けてコード全行の再精査を行ったところ、内部データ残留、Enterキー押下時の誤送信、タイマー重複、Safariでのクリップボード選択失敗、300行超過など、多数の重大な潜在バグや見落としが後から判明した。

### 1.2 ユーザーからの本質的な問題提起
1. **「表面的な形式チェックにとどまっていたのではないか」**:
   従来のMCPツールを用いた4段階監査は、静的ファイル構造やGitインデックスの整合性のみを検証する形式的チェックに偏っており、ブラウザ上で実際に動作させた際の動的バグを検知できていなかった。
2. **「MCPツール側の改善として、4段階の強化または5段階監査を新設し、見逃しをなくすべき」**:
   各プロジェクトで個別に場当たり的な修正を行うのではなく、MCPツールそのものの監査エンジンにバグ・エッジケースの自動検出ロジックを組み込むべきである。
3. **「V2だけでなくV3のMCPツールにも問題はないのか」**:
   現行のAI開発コンテキスト管理MCPツール（V2）だけでなく、最新のAIコンテキスト管理ツールV3（V3）のMCPツールにおいても、全く同様の見落としリスクが存在していないかの調査が求められた。
4. **「個別ツールの監査に引きずられず、汎用的な仕組みとすべき」**:
   特定ツール限定のテストスクリプトではなく、今後開発するあらゆるプロジェクト（Webフロントエンド、Node.jsバックエンド、CLIツール等）に共通して適用できる標準監査フレームワークを設計すべきである。
5. **「誇大表現（完全、100%自動完結等）を排した客観的評価と、絵文字の完全排除」**:
   プロトコル第4条に基づき、技術的限界を正確に認識した客観的な評価を行い、すべての絵文字・過剰な修飾語を排除すること。

---

## 2. 現行MCPツール（V2・V3）の内部構造分析と限界

### 2.1 AI開発コンテキスト管理MCPツール (V2) の分析
対象パス: `C:\Users\tk030\Desktop\AI開発コンテキスト管理MCPツール`
- **現行実装**: `buildQualityAuditPack.ts`, `audit.ts`, `deep-audit.ts`, `checkIntegrity.ts`
- **解析範囲**: ファイルサイズ（100KB超の警告）、モジュール結合度（依存先8個超の警告）、クラス内メソッド数（15個超の警告）、Gitインデックス乖離、Windowsパスのハードコード、Node.js exec のバッファ上限欠落。
- **欠落領域**: JavaScript/HTMLのコード内部におけるイベントリスナーの記述、非同期タイマー管理、DOM IDの整合性、ステータス変更時のメモリクリーンアップ、フォーム送信制御などは一切走査されていない。

### 2.2 AIコンテキスト管理ツールV3 (V3) の分析
対象パス: `C:\Users\tk030\Desktop\AIコンテキスト管理ツールV3`
- **現行実装**: `src/mcp/tools/analyze-tools.ts` (`v3.analyze_specification`, `v3.check_gate`, `v3.run_pre_audit`, `v3.check_drift`)
- **解析範囲**: 仕様書の曖昧語（「適宜」「適切に」等）の検出、プロトコル規約遵守（第6条事前承認、第16条Zero-Dependency等）、アーキテクチャの有向非巡回グラフ（DAG）構造、仕様書とGitコミット間のSpec Drift検出。
- **欠落領域**: 仕様やガバナンス規則のメタ監査には優れているが、**生成されたターゲットソースコードの具体的なランタイムバグ・UIエッジケースを検証するエンジンはV3にも存在しない**。したがって、V3のMCPツールを用いても今回の動的バグは同様に見逃される。

---

## 3. 静的監査で見逃される「8大潜在リスク領域」の定義

今後の監査エンジンが必ず網羅すべき、動的・ランタイム・環境依存のリスク領域は以下の8点である。

1. **メモリリーク・イベントリスナー多重累積**:
   画面遷移やリスト再描画時にイベントリスナーが多重登録され、1回の操作でイベントが二重・三重に発火する不具合。
2. **非同期処理のレースコンディション（競合状態）**:
   PromiseやクリップボードAPIの完了待ちの間に画面を切り替えた際、未マウント要素への操作によるコンソール例外停止。
3. **モバイル（iOS Safari / Android）ビューポート・キーボード干渉**:
   ソフトウェアキーボード表示時のボタン隠蔽、および `<input>` のフォントサイズが16px未満の際に発生するiOS特有の強制自動拡大ズームによるUI崩壊。
4. **ブラウザストレージの例外クラッシュ**:
   シークレットモード等における `localStorage` アクセス時の `SecurityError` によるアプリ起動停止。
5. **サロゲートペア・制御文字によるデータ破損**:
   絵文字や特殊記号、改行コード混入時の文字数カウント狂いおよびテキスト保存処理での文字化け。
6. **DOM属性インジェクション・XSSの抜け穴**:
   `innerHTML` のエスケープ漏れのみならず、`setAttribute('href', ...)` 等にユーザー入力が展開された場合の `javascript:` 実行脆弱性。
7. **フォーカス喪失・アクセシビリティ破綻**:
   モーダルやフォーム開閉時にTabキーフォーカスが `body` 先頭に飛ばされ、キーボード操作が不能になる不具合。
8. **ブラウザ間・OS間のフォントレンダリング差異**:
   OSごとのデフォルトフォント描画差によるボタン文字の改行落ちやレイアウトはみ出し。

---

## 4. 汎用「5段階品質・堅牢性監査フレームワーク」の体系

4段階監査を各段階で強化する案と、5段階監査を新設する案を比較検討した結果、**「エッジケース・ランタイムバグ専門のゲートを独立新設する5段階監査」** が最も見逃し防止力と保守性に優れていると結論付けた。

```text
┌────────────────────────────────────────────────────────────────────────┐
│                   5段階品質・堅牢性監査フレームワーク                  │
├────────────────────────────────────────────────────────────────────────┤
│ 【第1段階】コード構文・静的構造品質監査 (Syntax & Static Structure)    │
│   ・言語構文エラーゼロ、未定義参照ゼロ                                 │
│   ・プロトコル第17条（全ファイル300行以内ルール）の機械的適合判定      │
│   ・DOM ID整合性（HTML内の全idとJS内のgetElementByIdの100%突合）       │
│   ・セマンティックHTMLマークアップ & OGP/SEOメタタグ                   │
├────────────────────────────────────────────────────────────────────────┤
│ 【第2段階】プロトコル・アーキテクチャ適合監査 (Protocol & Governance) │
│   ・シニアプログラマープロトコル全18条（事前承認、Zero-Dep、第18条UI） │
│   ・単一責任モジュール分割、循環依存排除 (DAG構造)                     │
│   ・MITライセンス条項・著作権表示の整合性                              │
├────────────────────────────────────────────────────────────────────────┤
│ 【第3段階】仕様書・機能要件完全性監査 (Specification Completeness)     │
│   ・仕様書（全条項）の要件網羅性、コード実装との1対1対応検証          │
│   ・画面遷移フロー（Hero ➔ 入力 ➔ プロンプト ➔ リスト ➔ 判定）整合    │
│   ・AI質問文・修正依頼文生成エンジンの出力フォーマット検証             │
├────────────────────────────────────────────────────────────────────────┤
│ 【第4段階】深層ランタイム・エッジケース堅牢性監査 (Runtime & Robustness)│
│   ・イベント・非同期安全:                                              │
│       - ボタン連打時のタイマー重複防止 (clearTimeout)                  │
│       - Enterキー押下時のフォーム誤送信・リロード防止 (preventDefault) │
│       - Escapeキー等のモーダル・フォーム解除制御                       │
│   ・データ・状態一貫性:                                                │
│       - ステータス変更時の内部プロパティクリーンアップ (残留防止)      │
│       - 動的追加・削除時の配列インデックス・ID一貫性                   │
│   ・プラットフォーム例外耐性:                                          │
│       - クリップボードAPI拒否・非セキュア環境対応 (execCommand)        │
│       - ストレージ例外 (シークレットモード等)                          │
│   ・モバイル・ビューポート適合:                                        │
│       - iOS Safari 自動ズーム防止 (全入力要素 font-size: 16px 以上)    │
│       - 画面幅320px極小画面での文字欠損・ボタン重なり防止              │
├────────────────────────────────────────────────────────────────────────┤
│ 【第5段階】セキュリティ・公開適合性監査 (Security & Release)           │
│   ・機密情報（APIキー、トークン）ゼロ                                  │
│   ・Zero-Network安全性（完全ローカル動作・外部通信ゼロ）               │
│   ・GitHub Pages静的配信用設定 (.nojekyll、相対パス参照)               │
│   ・総合公開判定 (Grade A+ 認定および公開可否判定)                     │
└────────────────────────────────────────────────────────────────────────┘
```

---

## 5. 多段階判定モデル（リスクシグナル設計）

機械的な静的検出と、意味解釈が必要な項目の混同を防ぐため、監査エンジンの出力判定を以下の**4値判定モデル**として設計する。

| 判定ステータス | 定義・性格 | アクション・位置づけ |
|---|---|---|
| **PASS** | 機械的に完全検証・保証できた項目 | そのまま公開承認可能 |
| **ERROR** | 明らかな実装欠陥・破壊的リスク・規約違反 | 修正必須（ビルド・公開ブロック） |
| **WARNING** | 潜在的な不具合要因（特定環境や操作でのみ発現） | 警告を出力し、修正または許容判断を促す |
| **NEEDS_REVIEW** | 構文は存在するが、実行コンテキスト・意図解釈が必要 | **人間や外部AIによる精読・確認を推奨** |

### 「危険箇所検出器」としての動作原則
- `clearTimeout` や `preventDefault` が検出された場合でも、無条件に `PASS` とせず、文脈・スコープに応じて `NEEDS_REVIEW` を出力することで、**誤った安心感（False Positive）を防止**する。
- 定型的な見落としは本MCPツールで機械的に排除し、人間や外部AIによるレビューを「本当に解釈が必要な高難度箇所」に集中させる。

---

## 6. 5つの独立専門エージェント（ロール分離）体制の設計

単一AIによる甘い自己レビュー（確証バイアス）を排除するため、5つの独立した専門ロールによる合議監査体制をとる。

1. **Syntax & Static Inspector（構文・静的品質担当）**:
   文法エラー、300行ルール、DOM ID突合のみを機械的に精査。
2. **Protocol Compliance Inspector（プロトコル統制担当）**:
   プロトコル全18条（事前承認、Zero-Dependency、第18条デザイン標準、MITライセンス）の条文違反のみを厳格監査。
3. **Specification Completeness Inspector（仕様網羅担当）**:
   仕様書全条項とコード実装の1対1対応、画面フロー整合性のみを検証。
4. **Red-Team & Robustness Inspector（レッドチーム・破壊テスト担当）★最重要★**:
   「アプリを意図的に壊す」敵対的視点に特化し、連打、Enter送信、特殊入力、状態残留、API遮断、320px縮小を集中的に攻撃テスト。
5. **Security & Release Gatekeeper（公開判定ゲートキーパー）**:
   セキュリティ、Zero-Network、公開設定を検証し、各エージェントの判定を集約して最終合議判定を下す。

---

## 7. 各MCPツール本体への具体的な改修仕様（TypeScriptコード）

### 7.1 『AI開発コンテキスト管理MCPツール』(V2) の改修仕様
対象パス: `C:\Users\tk030\Desktop\AI開発コンテキスト管理MCPツール`

#### (1) `src/lib/parser/runtime-analyzer.ts`（新規作成）
以下の7つの静的解析ルールおよびリスクシグナル（PASS/ERROR/WARNING/NEEDS_REVIEW）判定を実装する。
- **Rule 1 (DOM ID突合)**: HTML内の `id` と JS内の `document.getElementById` の不一致を検出 (ERROR)。
- **Rule 2 (300行制限)**: 全ソースファイルの行数をカウントし、300行超過を警告 (ERROR/WARNING)。
- **Rule 3 (Enterキー送信防止)**: `e.key === 'Enter'` 処理箇所での `e.preventDefault()` 欠落を検出 (WARNING/NEEDS_REVIEW)。
- **Rule 4 (タイマー重複防止)**: `setTimeout`/`setInterval` 呼び出しにおける `clearTimeout` 保持の有無を検出 (WARNING/NEEDS_REVIEW)。
- **Rule 5 (API例外フォールバック)**: `navigator.clipboard` 呼び出し時の `catch` / `execCommand` フォールバック欠落を検出 (WARNING)。
- **Rule 6 (モバイルフォント基準)**: CSS内の入力要素 `font-size` が16px（1rem）未満である記述を検出 (WARNING)。
- **Rule 7 (公開設定)**: `.nojekyll` の配置有無、ルート相対パス使用を検出 (ERROR/PASS)。

#### (2) `src/lib/formatters/audit.ts` および `src/tools/buildQualityAuditPack.ts`（修正）
- 監査出力形式を「5段階品質・堅牢性監査」に改定。
- `runtime-analyzer` による自動解析結果を「第4段階：ランタイム・エッジケース監査」としてレポートに統合。

---

### 7.2 『AIコンテキスト管理ツールV3』(V3) の改修仕様
対象パス: `C:\Users\tk030\Desktop\AIコンテキスト管理ツールV3`

#### (1) `src/plugins/audit/runtime-rule-engine.ts`（新規作成）
V3の監査プラグイン内に、コードレベルの動的リスク（DOM ID突合、Enter送信対策、タイマー重複、フォールバック例外）を評価するルールエンジンをプラグイン形式で実装。

#### (2) `src/mcp/tools/analyze-tools.ts`（修正）
- `v3.check_gate` および `v3.run_pre_audit` に、コードレベルのランタイム堅牢性ゲート（`GATE-007: Runtime Robustness & Event Safety Check`）を追加。

---

## 8. ビルド・動作検証手順

1. **AI開発コンテキスト管理MCPツール**:
   `npm run typecheck` ➔ `npm run build` を実行し `dist/index.js` を更新。
2. **AIコンテキスト管理ツールV3**:
   `npm run typecheck` ➔ `npm test` ➔ `npm run build` を実行。
3. **結合確認**:
   MCP Inspector 等を用いて、拡張された5段階監査ツールが正常にランタイムリスクを検出することを確認。
